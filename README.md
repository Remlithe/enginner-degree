# parkcheck

A new Flutter project.

utwórz pusty folder
w pustym folderze otwórz terminal
w terminalu:
git init -> git remote add origin git@github.com:Remlithe/enginner-degree.git -> git pull origin main
otwórz folder w studio code
wpisz w terminal w studio code (emulator został stowrzony za pomocą android studio): 
flutter pub get -> flutter emulators --launch nazwa_twojego_emulatora -> flutter run
